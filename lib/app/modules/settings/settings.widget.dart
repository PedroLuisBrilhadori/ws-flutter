import 'package:flutter/cupertino.dart';

class SettingsWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Configurações Page'));
  }
}
